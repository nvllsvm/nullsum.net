#!/usr/bin/env sh
COMPOSE_VERSION=1.24.1
exec docker run \
    --rm \
    -v /var/run/docker.sock:/var/run/docker.sock \
    -v "${PWD}:${PWD}" \
    -w "${PWD}" \
    "docker/compose:${COMPOSE_VERSION}" "$@"
